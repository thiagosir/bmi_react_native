import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    marginBottom: 24,
  },
  label: {
    fontSize: 18,
    color: "#FFF",
    marginBottom: 8,
  },
  input: {
    backgroundColor: "#DDD",
    borderRadius: 8,
    height: 50,
    width: "100%",
    paddingBottom: 24,
    fontSize: 16,
    borderColor: "#5E60CE",
  },
});
